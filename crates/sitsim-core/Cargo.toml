[package]
name = "sitsim-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "6-DOF quadrotor-X dynamics, quaternion propagation, PCG64 RNG (SPEC §5, §8.1)"

[dependencies]
libm = "0.2"
