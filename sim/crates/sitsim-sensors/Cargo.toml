[package]
name = "sitsim-sensors"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Sensor models: BMI088-class IMU, magnetometer, barometer, GPS with latency queue, battery (SPEC §6.3-6.5)"

[dependencies]
sitsim-core = { path = "../sitsim-core" }
sitsim-env = { path = "../sitsim-env" }
libm = "0.2"
